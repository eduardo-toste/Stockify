name: Deploy - Stockify

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Build da aplicação
        run: mvn clean package -DskipTests=true

      - name: Copiar .jar para EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./target/stockify-0.0.1-SNAPSHOT.jar"
          target: "/home/ec2-user/"
          strip_components: 0

      - name: Verificar arquivo .jar na EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "📁 Verificando existência do .jar..."
            ls -lah /home/ec2-user/stockify-0.0.1-SNAPSHOT.jar
            echo "🔐 Hash do .jar (para validação):"
            sha256sum /home/ec2-user/stockify-0.0.1-SNAPSHOT.jar || echo "Arquivo não encontrado"

      - name: Reiniciar aplicação via SSH com tmux
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🔧 Instalando tmux (se necessário)..."
            sudo yum install -y tmux

            echo "🛑 Matando sessões anteriores..."
            pkill -f 'stockify-0.0.1-SNAPSHOT.jar' || true
            tmux kill-session -t stockify || true

            echo "🚀 Iniciando nova sessão tmux com aplicação..."
            tmux new-session -d -s stockify "java -Dspring.profiles.active=prod -jar /home/ec2-user/stockify-0.0.1-SNAPSHOT.jar"

            sleep 5
            PID=$(pgrep -f stockify)
            if [ -n "$PID" ]; then
              echo "✅ Aplicação iniciada com PID $PID na sessão tmux 'stockify'"
            else
              echo "❌ Falha ao iniciar aplicação"
              tail -n 30 output.log || true
              exit 1
            fi